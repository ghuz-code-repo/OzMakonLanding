version: '3.8'

services:
  ozmakon:
    build:
      context: .
      dockerfile: Dockerfile.simple
    container_name: ozmakon-prod
    restart: unless-stopped
    ports:
      - '6560:80'
    environment:
      - NODE_ENV=production

